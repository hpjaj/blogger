<div class="container">
  <div>
    <%= link_to "New Blog Post", new_post_path %>
  </div>
  <div>
    <% @posts.each do |post| %>
      <div>
        <p><%= link_to post.title, post %></p>
        <p><%= post.body %></p>
      </div>
    <% end %>
  </div>
</div>
