<div class="container">
  <%= form_for @post do |f| %>
    <p><%= f.text_field :title, placeholder: "Title" %></p>
    <p><%= f.text_area :body, placeholder: "Body", size: "60x12" %></p>
    <p><%= f.submit "Save Blog Post" %></p>
  <% end %>
  <p><%= link_to "Cancel", :back %></p>
</div>